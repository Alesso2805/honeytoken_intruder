version: '3.8'

services:
  honeytoken_sentinel:
    build: .
    container_name: honeytoken-microservice
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./instance:/app/instance
    restart: always

volumes:
  sqlite_data:
